<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bcs_preparation">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BCS_preparation">
        <activity
            android:name=".view.Dashboard"
            android:exported="false" />
        <activity
            android:name=".view.authentication.Forgorpassword"
            android:exported="false" />
        <activity
            android:name=".view.authentication.Singup"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.authentication.Login"
            android:exported="true" >

        </activity>
        <activity
            android:name=".view.MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>